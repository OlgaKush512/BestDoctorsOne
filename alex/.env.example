# One provider is enough: OpenAI or Blackbox
# OpenAI provider
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini

# Blackbox provider
# BLACKBOX_API_KEY=sk-...
# BLACKBOX_BASE_URL=https://api.blackbox.ai
# BLACKBOX_MODEL=blackbox-omni
# Optionally provide extra header if your provider needs it in addition to Authorization
# BLACKBOX_EXTRA_HEADER_NAME=X-API-KEY
# BLACKBOX_EXTRA_HEADER_VALUE=sk-...

# Debug switches
DEBUG=1
DRY_RUN=0
BYPASS_LLM=0

# MCP Playwright server launcher
# Use npx + published package (recommended):
MCP_PLAYWRIGHT_COMMAND=npx
MCP_PLAYWRIGHT_ARGS=@modelcontextprotocol/server-playwright@latest

# If you want to run a local server file, switch to node and point to a JS file:
# MCP_PLAYWRIGHT_COMMAND=node
# MCP_PLAYWRIGHT_ARGS=./path/to/server.js

